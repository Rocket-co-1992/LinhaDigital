{
  "name": "easy-site-builder",
  "version": "1.0.0",
  "description": "A simple site builder for small entrepreneurs, freelancers, agencies, and SMEs.",
  "main": "index.html",
  "scripts": {
    "start": "npx live-server --port=3000 --host=0.0.0.0",
    "dev": "npm run build:dev && npm run start",
    "build": "npm run build:css && npm run build:js",
    "build:dev": "npm run build:css:dev && npm run build:js:dev",
    "build:css": "cd assets && tailwindcss -i ./css/app.css -o ../build/css/app.css --minify",
    "build:css:dev": "cd assets && tailwindcss -i ./css/app.css -o ../build/css/app.css",
    "build:js": "webpack --mode=production",
    "build:js:dev": "webpack --mode=development"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
